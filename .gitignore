# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Tool caches (ruff, pytest, coverage)
.ruff_cache/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual environments
.venv
venv/

# Secrets / environment (keep .env.example tracked)
.env
.env.*
!.env.example

# Local session data
.pi/

# Factory scratch state only (current run pointer). Tasks, evaluations and
# config are the lab notebook and stay versioned (ADR-0001: evidence first).
.factory/state/

# Local tooling (speckit/specify scaffolding)
.specify/

# Mutation testing workspace (mutmut)
mutants/
.mutmut-cache/

# === Universal / editor / OS patterns ===
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors / IDEs
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# Backup / misc
*.bak
*.orig
*.tmp

# Python packaging artifacts not already covered
*.egg
*.whl
*.tar.gz

# Test / coverage extras
.htmlcov/
.coverage
.coverage.*
coverage.xml
*.py[co]  # (already above, kept for clarity)

# OS-generated cruft
**/.DS_Store
